## Customize the test machine
machine:

  services:
    # - docker

  timezone:
    America/Los_Angeles # Set the timezone

dependencies:
  pre:
    - pip install -r django-polls/requirements.txt

general:
  build_dir: django-polls

